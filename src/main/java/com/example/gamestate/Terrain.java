package com.example.gamestate;

/**
 * an enum to represent the type og terrain on a tile.
 */
public enum Terrain {
    /**
     * grass path and tile representing those respective types of terrain.
     */
    Grass, Path, Tunnel
}
